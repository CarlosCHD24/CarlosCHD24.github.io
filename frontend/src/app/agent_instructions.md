# App Router Instructions

1. Keep `/` and `/empleo-publico/` statically exportable and functionally equivalent.
2. Preserve `main#main-content`, the Spanish skip link, metadata, sitemap, robots, and WebSite structured data.
3. Use the helpers in `src/lib/site.ts` for canonical URLs.
4. Source factual copy from `../../../../context-in-text/empleo_publico_visor.md`.
5. Do not restore the retired portfolio routes or personal structured data.
